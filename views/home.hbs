{{!-- {{data}} --}}
====================
{{#each data}}
  <p>
    <span> {{this.name}} </span>
    <a href='/games/{{this.name}}'>
      <img src='{{this.image}}' />
    </a>
  </p>
{{/each}}
======================